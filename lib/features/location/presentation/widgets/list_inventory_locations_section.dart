import 'package:flutter/material.dart';

class ListInventoryLocationsSection extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('LIST INVENTORIES'),
    );
  }
}